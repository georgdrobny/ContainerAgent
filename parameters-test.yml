trigger: none

parameters:
- name: AgentImage
  displayName: Pipeline Agent Image
  type: string
  default: windows2019
  values:
    - windows2019
    - windows2022

variables:
  imageSelection: $[${{ parameters.AgentImage }}]

jobs:
- job: 
  pool:
    vmImage: ${{ parameters.AgentImage }}
  steps:
  - pwsh: Write-Host "${{ parameters.AgentImage}}"

